angular.module('myApp').controller('NavCtrl', ['$scope'], '$location', '$modal', 'hotkeys', 'use strict';

       $.getScript('/src/app/app.components.ts', removeSelected()

       hotkeys.add({
         combo: 'a',
         callback: function() {
           removeSelected()
         }
       })

)
)
